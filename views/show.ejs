<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= blog.title %> - My Blog</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <header>
        <nav>
            <a href="/" class="logo">✍️ My Blog</a>
            <ul class="nav-links">
                <li><a href="/">Home</a></li>
                <li><a href="/new">Write Post</a></li>
            </ul>
            <form class="search-form" action="/search" method="GET">
                <input type="text" name="q" placeholder="Search blogs..." required>
                <button type="submit">🔍</button>
            </form>
        </nav>
    </header>

    <main>
        <article class="blog-post">
            <div class="blog-post-header">
                <h1><%= blog.title %></h1>
                <div class="blog-post-meta">
                    <div>
                        <span class="blog-author">By <%= blog.author %></span>
                        <span class="blog-date">Published on <%= blog.createdAt.toLocaleDateString() %></span>
                        <% if (blog.updatedAt > blog.createdAt) { %>
                            <span class="blog-date">Updated on <%= blog.updatedAt.toLocaleDateString() %></span>
                        <% } %>
                    </div>
                    <% if (blog.tags.length > 0) { %>
                        <div class="blog-tags">
                            <% blog.tags.forEach(tag => { %>
                                <span class="tag"><%= tag %></span>
                            <% }) %>
                        </div>
                    <% } %>
                </div>
            </div>

            <div class="blog-post-content">
                <% 
                    const paragraphs = blog.content.split('\n').filter(p => p.trim() !== '');
                    paragraphs.forEach(paragraph => { 
                %>
                    <p><%= paragraph %></p>
                <% }) %>
            </div>

            <div class="blog-post-actions">
                <a href="/" class="btn btn-secondary">← Back to Home</a>
                <a href="/blogs/<%= blog._id %>/edit" class="btn btn-primary">Edit Post</a>
                <form style="display: inline;" action="/blogs/<%= blog._id %>?_method=DELETE" method="POST" onsubmit="return confirm('Are you sure you want to delete this post?')">
                    <button type="submit" class="btn btn-danger">Delete Post</button>
                </form>
            </div>
        </article>
    </main>

    <footer>
        <p>&copy; 2025 My Blog. Built with Node.js, Express.js, and MongoDB.</p>
    </footer>
</body>
</html>