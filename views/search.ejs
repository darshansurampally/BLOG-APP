<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Results - My Blog</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <header>
        <nav>
            <a href="/" class="logo">✍️ My Blog</a>
            <ul class="nav-links">
                <li><a href="/">Home</a></li>
                <li><a href="/new">Write Post</a></li>
            </ul>
            <form class="search-form" action="/search" method="GET">
                <input type="text" name="q" placeholder="Search blogs..." value="<%= query %>" required>
                <button type="submit">🔍</button>
            </form>
        </nav>
    </header>

    <main>
        <div class="page-header">
            <h1>Search Results</h1>
            <p>Found <%= blogs.length %> result(s) for "<%= query %>"</p>
        </div>

        <% if (blogs.length === 0) { %>
            <div class="empty-state">
                <h2>No results found</h2>
                <p>Try searching with different keywords or <a href="/" style="text-decoration: none;">browse all posts</a></p>
                <a href="/" class="btn btn-primary">Back to Home</a>
            </div>
        <% } else { %>
            <div class="blog-grid">
                <% blogs.forEach(blog => { %>
                    <article class="blog-card">
                        <div class="blog-card-header">
                            <h2><a href="/blogs/<%= blog._id %>"><%= blog.title %></a></h2>
                            <div class="blog-meta">
                                <span class="blog-author">By <%= blog.author %></span>
                                <span class="blog-date"><%= blog.createdAt.toLocaleDateString() %></span>
                            </div>
                            <p class="blog-excerpt">
                                <%= blog.content.substring(0, 150) %><%= blog.content.length > 150 ? '...' : '' %>
                            </p>
                            <% if (blog.tags.length > 0) { %>
                                <div class="blog-tags">
                                    <% blog.tags.forEach(tag => { %>
                                        <span class="tag"><%= tag %></span>
                                    <% }) %>
                                </div>
                            <% } %>
                        </div>
                        <div class="blog-card-footer">
                            <a href="/blogs/<%= blog._id %>" class="btn btn-primary btn-small">Read More</a>
                            <div>
                                <a href="/blogs/<%= blog._id %>/edit" class="btn btn-secondary btn-small">Edit</a>
                                <form style="display: inline;" action="/blogs/<%= blog._id %>?_method=DELETE" method="POST" onsubmit="return confirm('Are you sure you want to delete this post?')">
                                    <button type="submit" class="btn btn-danger btn-small">Delete</button>
                                </form>
                            </div>
                        </div>
                    </article>
                <% }) %>
            </div>
        <% } %>
    </main>

    <footer>
        <p>&copy; 2025 My Blog. Built with Node.js, Express.js, and MongoDB.</p>
    </footer>
</body>
</html>